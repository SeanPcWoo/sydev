{
  "name": "sydev",
  "version": "0.1.0",
  "private": true,
  "description": "SylixOS 开发环境快速部署工具",
  "engines": {
    "node": ">=24.14.0"
  },
  "scripts": {
    "dev:cli": "pnpm --filter @sydev/cli dev",
    "build": "pnpm -r build",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "tsx": "^4.0.0",
    "vitest": "^3.0.0"
  }
}
