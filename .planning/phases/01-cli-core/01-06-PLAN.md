---
phase: 01-cli-core
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/env-checker.ts
autonomous: true
requirements:
  - CLI-ENV-02
  - CLI-ENV-03
gap_closure: true

must_haves:
  truths:
    - "环境检查器能够正确检测 RealEvo-Stream 工具链（rl-workspace、rl-project 等命令）"
    - "用户在环境检查失败时收到清晰的中文错误提示"
    - "用户在环境检查失败时收到具体的修复建议"
  artifacts:
    - path: "packages/core/src/env-checker.ts"
      provides: "RealEvo-Stream 工具链检测逻辑"
      min_lines: 100
      exports: ["checkRlCommand", "checkToolchain", "checkEnvironment"]
  key_links:
    - from: "packages/cli/src/index.ts"
      to: "packages/core/src/env-checker.ts"
      via: "import { checkEnvironment }"
      pattern: "checkEnvironment\\(\\)"
---

<objective>
修复环境检查器的命令检测逻辑和错误消息，使其能够正确检测 RealEvo-Stream 工具链的实际命令（rl-workspace、rl-project 等），并提供清晰的中文错误提示和具体的修复建议。

Purpose: 解决 UAT Test 1 发现的阻塞性问题，使环境检查器能够正确工作，解除对后续交互式向导测试的阻塞。

Output: 修复后的 env-checker.ts，能够正确检测 rl-workspace 等实际命令，提供中文错误消息和具体修复建议。
</objective>

<execution_context>
@/home/sean/.claude/get-shit-done/workflows/execute-plan.md
@/home/sean/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sean/project/openswitch/.planning/PROJECT.md
@/home/sean/project/openswitch/.planning/ROADMAP.md
@/home/sean/project/openswitch/.planning/STATE.md
@/home/sean/project/openswitch/.planning/phases/01-cli-core/01-UAT.md
@/home/sean/project/openswitch/.planning/debug/env-check-wrong-command.md
</context>

<interfaces>
<!-- 当前 env-checker.ts 导出的接口 -->

From packages/core/src/env-checker.ts:
```typescript
export function checkRlCommand(): EnvCheckResult;
export function checkToolchain(): ToolchainCheckResult;
export function checkEnvironment(): EnvironmentStatus;
```

From packages/core/src/types.ts (推断):
```typescript
interface RlVersion {
  major: number;
  minor: number;
  patch: number;
  raw: string;
}

interface EnvCheckResult {
  available: boolean;
  version?: RlVersion;
  error?: string;
  fixSuggestion?: string;
}

interface ToolchainCheckResult {
  installed: boolean;
  path?: string;
  error?: string;
}

interface EnvironmentStatus {
  rl: EnvCheckResult;
  toolchain: ToolchainCheckResult;
  overall: boolean;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 修复 checkRlCommand() 检测逻辑和错误消息</name>
  <files>packages/core/src/env-checker.ts</files>
  <action>
修改 checkRlCommand() 函数，解决以下问题：

1. **修改命令检测逻辑（第17行）**：
   - 将 `rl --version` 改为检测实际存在的命令 `rl-workspace --version`
   - 根据 PROJECT.md 第45行，RealEvo-Stream 的核心命令是 `rl-workspace`、`rl-project`、`rl-device`、`rl-build`，不存在统一的 `rl` 命令

2. **中文化所有错误消息**：
   - 第31行：将 "Failed to parse version from rl command output" 改为 "无法从 rl-workspace 命令输出中解析版本号"
   - 第51行：将 "rl command not found in PATH" 改为 "未找到 rl-workspace 命令"
   - 第58行：将 "Failed to execute rl command" 改为 "执行 rl-workspace 命令失败"

3. **提供具体的修复建议**：
   - 第32行：将 "Please ensure RealEvo-Stream toolchain is properly installed" 改为 "请确保 RealEvo-Stream 工具链已正确安装。检查以下命令是否可用：rl-workspace, rl-project, rl-device, rl-build"
   - 第52行：将 "Please install RealEvo-Stream toolchain and ensure rl command is in your PATH. Visit https://realevo.com for installation instructions." 改为 "请安装 RealEvo-Stream 工具链并确保以下命令在 PATH 中：rl-workspace, rl-project, rl-device, rl-build。安装后请运行 'which rl-workspace' 验证。"
   - 第59行：将 "Please check your RealEvo-Stream installation" 改为 "请检查 RealEvo-Stream 安装。尝试手动运行 'rl-workspace --version' 查看详细错误信息。"

4. **更新函数注释（第13行）**：
   - 将 "检查 rl 命令是否可用并解析版本号" 改为 "检查 rl-workspace 命令是否可用并解析版本号"

保持函数签名和返回类型不变，确保与现有代码兼容。
  </action>
  <verify>
    <automated>cd /home/sean/project/openswitch && pnpm test -- env-checker.test.ts</automated>
  </verify>
  <done>
checkRlCommand() 函数检测 rl-workspace 命令，所有错误消息为中文，修复建议具体可操作，现有测试通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新 checkToolchain() 错误消息为中文</name>
  <files>packages/core/src/env-checker.ts</files>
  <action>
修改 checkToolchain() 函数的错误消息为中文：

1. **第95行错误消息**：
   - 将 "RealEvo-Stream toolchain not found in common installation paths" 改为 "未在常见安装路径中找到 RealEvo-Stream 工具链"

2. **添加具体的修复建议**：
   - 在 ToolchainCheckResult 返回时添加 fixSuggestion 字段（如果类型支持）
   - 建议内容："请检查以下路径：/opt/realevo, ~/realevo, ~/.realevo，或设置环境变量 REALEVO_HOME 指向安装目录"

保持函数逻辑不变，仅更新错误消息语言和详细程度。
  </action>
  <verify>
    <automated>cd /home/sean/project/openswitch && pnpm test -- env-checker.test.ts</automated>
  </verify>
  <done>
checkToolchain() 函数错误消息为中文，提供具体的路径检查建议，现有测试通过。
  </done>
</task>

<task type="auto">
  <name>Task 3: 验证修复效果</name>
  <files>packages/core/src/env-checker.ts</files>
  <action>
运行完整的测试套件和手动验证：

1. **运行所有 core 包测试**：
   ```bash
   cd /home/sean/project/openswitch && pnpm test
   ```

2. **手动测试环境检查**（如果系统有 rl-workspace 命令）：
   ```bash
   cd /home/sean/project/openswitch && pnpm dev:cli workspace status
   ```
   预期：如果 rl-workspace 存在，应该通过环境检查；如果不存在，应该显示中文错误消息和具体修复建议。

3. **验证错误消息质量**：
   - 所有错误消息为中文
   - 修复建议具体可操作（包含命令示例、路径示例）
   - 错误消息清晰易懂，无技术术语堆砌
  </action>
  <verify>
    <automated>cd /home/sean/project/openswitch && pnpm test</automated>
  </verify>
  <done>
所有测试通过，环境检查器能够正确检测 rl-workspace 命令，错误消息为中文且具体可操作。
  </done>
</task>

</tasks>

<verification>
1. checkRlCommand() 检测 `rl-workspace --version` 而不是 `rl --version`
2. 所有错误消息为中文
3. 所有修复建议具体可操作，包含命令示例或路径示例
4. 现有测试套件全部通过
5. 手动测试环境检查功能正常
</verification>

<success_criteria>
- checkRlCommand() 函数检测实际存在的 rl-workspace 命令
- 所有错误消息使用中文
- 修复建议具体可操作，包含命令示例（如 'which rl-workspace'）和路径示例（如 /opt/realevo）
- 现有测试套件通过率 100%
- UAT Test 1 的阻塞问题解除，后续交互式向导测试可以继续
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-core/01-06-SUMMARY.md`
</output>
