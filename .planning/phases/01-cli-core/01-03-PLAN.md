---
phase: 01-cli-core
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/cli/src/index.ts
  - apps/cli/src/commands/workspace.ts
  - apps/cli/src/commands/project.ts
  - apps/cli/src/commands/device.ts
  - apps/cli/src/utils/help-formatter.ts
  - apps/cli/package.json
  - package.json
autonomous: true
requirements:
  - CLI-STRUCT-01
  - CLI-HELP-01
  - CLI-HELP-02
  - CLI-HELP-03

must_haves:
  truths:
    - "用户可以使用清晰的子命令结构（workspace、project、device）完成操作"
    - "用户可以通过 --help 查看所有命令的帮助信息"
    - "用户可以查看每个命令的使用示例"
    - "用户可以查看工具的版本信息"
  artifacts:
    - path: "apps/cli/src/index.ts"
      provides: "CLI 入口，Commander 主程序"
      min_lines: 50
    - path: "apps/cli/src/commands/workspace.ts"
      provides: "workspace 子命令（init, status）"
      min_lines: 40
    - path: "apps/cli/src/commands/project.ts"
      provides: "project 子命令（create, list）"
      min_lines: 40
    - path: "apps/cli/src/commands/device.ts"
      provides: "device 子命令（add, list）"
      min_lines: 40
    - path: "apps/cli/src/utils/help-formatter.ts"
      provides: "帮助信息格式化器，美化输出"
      min_lines: 30
  key_links:
    - from: "apps/cli/src/index.ts"
      to: "commander"
      via: "import and create program"
      pattern: "import.*Command.*from.*commander"
    - from: "apps/cli/src/commands/workspace.ts"
      to: "@openswitch/core"
      via: "import checkEnvironment"
      pattern: "import.*checkEnvironment.*@openswitch/core"
    - from: "apps/cli/src/index.ts"
      to: "apps/cli/package.json"
      via: "read version"
      pattern: "version.*package\\.json"
---

<objective>
建立 CLI 应用的命令结构和帮助系统，使用 Commander 实现清晰的子命令分组（workspace、project、device），并提供完善的帮助文档和版本信息。

Purpose: 为用户提供直观的命令行界面，清晰的命令结构降低学习成本，完善的帮助系统确保用户可以自助解决问题。

Output: 可执行的 CLI 工具骨架，包含三个主要子命令组和完整的帮助系统，为后续的交互式向导提供命令入口。
</objective>

<execution_context>
@/home/sean/.claude/get-shit-done/workflows/execute-plan.md
@/home/sean/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/01-cli-core/01-01-SUMMARY.md
</context>

<interfaces>
<!-- 从 Plan 01 提供的核心模块接口 -->
From packages/core/src/index.ts:
```typescript
// 环境检查
export function checkEnvironment(): Promise<EnvironmentStatus>;
export function checkRlCommand(): Promise<EnvCheckResult>;
export function checkToolchain(): Promise<ToolchainCheckResult>;

// 配置管理
export class ConfigManager {
  static validate<T>(schema: z.ZodSchema<T>, data: unknown): { valid: boolean; data?: T; errors?: string[] };
  static merge<T>(...configs: Partial<T>[]): T;
  static exportToJson<T>(config: T): string;
  static importFromJson<T>(schema: z.ZodSchema<T>, json: string): { valid: boolean; data?: T; errors?: string[] };
}

// rl 命令包装
export class RlWrapper {
  constructor(progressReporter: ProgressReporter);
  async initWorkspace(config: { baseVersion: string; platform: string; path: string }): Promise<RlCommandResult>;
  async createProject(config: { name: string; type: string; path: string }): Promise<RlCommandResult>;
}

// 进度报告
export class ProgressReporter extends EventEmitter {
  reportStep(name: string, progress: number): void;
  reportSuccess(message: string): void;
  reportError(error: string, stack?: string, fixSuggestion?: string): void;
}

// 类型
export interface EnvironmentStatus {
  rl: EnvCheckResult;
  toolchain: ToolchainCheckResult;
  overall: boolean;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 添加 CLI 依赖并配置入口</name>
  <files>
    apps/cli/package.json
    package.json
  </files>
  <action>
更新 apps/cli/package.json 添加 CLI 依赖：

```json
{
  "name": "@openswitch/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "openswitch": "./src/index.ts"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@openswitch/core": "workspace:*",
    "commander": "^12.1.0",
    "chalk": "^5.3.0",
    "ora": "^8.0.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "tsx": "^4.19.0"
  }
}
```

更新根目录 package.json 添加全局脚本：

```json
{
  "scripts": {
    "dev:cli": "pnpm --filter @openswitch/cli dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}
```

依赖说明：
- commander 12.x: CLI 框架，500M+ 周下载量
- chalk 5.x: 终端颜色输出
- ora 8.x: 进度 spinner 动画
  </action>
  <verify>
    <automated>cd apps/cli && pnpm install && pnpm list</automated>
  </verify>
  <done>
CLI 依赖添加完成，pnpm install 成功，commander、chalk、ora 正确安装。
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现 CLI 主程序和版本命令</name>
  <files>
    apps/cli/src/index.ts
    apps/cli/src/utils/help-formatter.ts
  </files>
  <action>
创建 CLI 主程序：

1. apps/cli/src/index.ts：
```typescript
#!/usr/bin/env node
import { Command } from 'commander';
import { readFileSync } from 'fs';
import { fileURLToPath } from 'url';
import { dirname, join } from 'path';
import chalk from 'chalk';
import { checkEnvironment } from '@openswitch/core';
import { workspaceCommand } from './commands/workspace.js';
import { projectCommand } from './commands/project.js';
import { deviceCommand } from './commands/device.js';
import { formatHelp } from './utils/help-formatter.js';

const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);
const packageJson = JSON.parse(
  readFileSync(join(__dirname, '../package.json'), 'utf-8')
);

const program = new Command();

program
  .name('openswitch')
  .description('SylixOS 开发环境快速部署工具')
  .version(packageJson.version, '-v, --version', '显示版本信息')
  .helpOption('-h, --help', '显示帮助信息')
  .configureHelp({
    formatHelp: (cmd, helper) => formatHelp(cmd, helper)
  });

// 全局环境检查选项
program.hook('preAction', async (thisCommand) => {
  // 跳过 --version 和 --help
  if (thisCommand.args.includes('--version') || thisCommand.args.includes('--help')) {
    return;
  }

  // 执行环境检查
  const envStatus = await checkEnvironment();
  if (!envStatus.overall) {
    console.error(chalk.red('✗ 环境检查失败'));
    if (!envStatus.rl.available) {
      console.error(chalk.yellow(`  rl 命令: ${envStatus.rl.error}`));
      console.error(chalk.cyan(`  建议: ${envStatus.rl.fixSuggestion}`));
    }
    if (!envStatus.toolchain.installed) {
      console.error(chalk.yellow(`  工具链: ${envStatus.toolchain.error}`));
    }
    process.exit(1);
  }
});

// 注册子命令
program.addCommand(workspaceCommand);
program.addCommand(projectCommand);
program.addCommand(deviceCommand);

// 解析命令行参数
program.parse();
```

2. apps/cli/src/utils/help-formatter.ts：
```typescript
import chalk from 'chalk';
import { Command, Help } from 'commander';

export function formatHelp(cmd: Command, helper: Help): string {
  const termWidth = process.stdout.columns || 80;
  const indent = '  ';

  let output = '';

  // 标题
  output += chalk.bold.cyan(`\n${cmd.name()}\n`);

  // 描述
  if (cmd.description()) {
    output += `\n${indent}${cmd.description()}\n`;
  }

  // 用法
  output += chalk.bold('\n用法:\n');
  output += `${indent}$ ${cmd.name()} ${helper.commandUsage(cmd)}\n`;

  // 命令列表
  const commands = cmd.commands.filter(c => !c.hidden);
  if (commands.length > 0) {
    output += chalk.bold('\n命令:\n');
    commands.forEach(c => {
      const name = c.name().padEnd(20);
      output += `${indent}${chalk.green(name)} ${c.description()}\n`;
    });
  }

  // 选项
  const options = helper.visibleOptions(cmd);
  if (options.length > 0) {
    output += chalk.bold('\n选项:\n');
    options.forEach(opt => {
      const flags = opt.flags.padEnd(25);
      output += `${indent}${chalk.yellow(flags)} ${opt.description}\n`;
    });
  }

  // 示例
  output += chalk.bold('\n示例:\n');
  output += `${indent}$ openswitch workspace init    ${chalk.dim('# 初始化 workspace')}\n`;
  output += `${indent}$ openswitch project create    ${chalk.dim('# 创建项目')}\n`;
  output += `${indent}$ openswitch device add        ${chalk.dim('# 添加设备')}\n`;
  output += `${indent}$ openswitch --help            ${chalk.dim('# 查看帮助')}\n`;

  output += '\n';
  return output;
}
```

注意：使用 chalk 美化输出，符合用户偏好"重视帮助信息的清晰度和美观度"。
  </action>
  <verify>
    <automated>cd apps/cli && pnpm dev --version && pnpm dev --help</automated>
  </verify>
  <done>
CLI 主程序创建完成，--version 显示版本号，--help 显示美化的帮助信息，环境检查在命令执行前自动运行。
  </done>
</task>

<task type="auto">
  <name>Task 3: 实现 workspace 子命令</name>
  <files>
    apps/cli/src/commands/workspace.ts
  </files>
  <action>
创建 workspace 子命令：

apps/cli/src/commands/workspace.ts：
```typescript
import { Command } from 'commander';
import chalk from 'chalk';
import { checkEnvironment } from '@openswitch/core';

export const workspaceCommand = new Command('workspace')
  .description('管理 SylixOS workspace')
  .addHelpText('after', `
示例:
  $ openswitch workspace init     # 交互式初始化 workspace
  $ openswitch workspace status   # 查看 workspace 状态
`);

workspaceCommand
  .command('init')
  .description('初始化 workspace（交互式向导）')
  .action(async () => {
    console.log(chalk.blue('ℹ workspace init 命令将在 Plan 04 实现交互式向导'));
    console.log(chalk.dim('当前为命令结构骨架'));
  });

workspaceCommand
  .command('status')
  .description('查看当前 workspace 状态')
  .action(async () => {
    console.log(chalk.cyan('检查 workspace 状态...'));

    // 简单实现：检查 .realevo 目录是否存在
    const fs = await import('fs');
    const path = await import('path');
    const workspacePath = path.join(process.cwd(), '.realevo');

    if (fs.existsSync(workspacePath)) {
      console.log(chalk.green('✓ Workspace 已初始化'));
      console.log(chalk.dim(`  路径: ${workspacePath}`));
    } else {
      console.log(chalk.yellow('⚠ Workspace 未初始化'));
      console.log(chalk.dim('  运行 openswitch workspace init 开始初始化'));
    }
  });
```

注意：init 命令当前只是骨架，实际的交互式向导将在 Plan 04 实现。status 命令提供简单的状态检查。
  </action>
  <verify>
    <automated>cd apps/cli && pnpm dev workspace --help</automated>
  </verify>
  <done>
workspace 子命令创建完成，包含 init 和 status 两个子命令，帮助信息清晰，命令可执行。
  </done>
</task>

<task type="auto">
  <name>Task 4: 实现 project 和 device 子命令</name>
  <files>
    apps/cli/src/commands/project.ts
    apps/cli/src/commands/device.ts
  </files>
  <action>
创建 project 和 device 子命令：

1. apps/cli/src/commands/project.ts：
```typescript
import { Command } from 'commander';
import chalk from 'chalk';

export const projectCommand = new Command('project')
  .description('管理 SylixOS 项目')
  .addHelpText('after', `
示例:
  $ openswitch project create     # 交互式创建项目
  $ openswitch project list       # 列出所有项目
`);

projectCommand
  .command('create')
  .description('创建新项目（交互式向导）')
  .action(async () => {
    console.log(chalk.blue('ℹ project create 命令将在 Plan 04 实现交互式向导'));
    console.log(chalk.dim('当前为命令结构骨架'));
  });

projectCommand
  .command('list')
  .description('列出当前 workspace 中的所有项目')
  .action(async () => {
    console.log(chalk.cyan('查找项目...'));

    const fs = await import('fs');
    const path = await import('path');
    const workspacePath = path.join(process.cwd(), '.realevo');

    if (!fs.existsSync(workspacePath)) {
      console.log(chalk.yellow('⚠ Workspace 未初始化'));
      return;
    }

    // 简单实现：列出包含 .rlproject 的目录
    const dirs = fs.readdirSync(process.cwd(), { withFileTypes: true })
      .filter(d => d.isDirectory() && fs.existsSync(path.join(process.cwd(), d.name, '.rlproject')))
      .map(d => d.name);

    if (dirs.length === 0) {
      console.log(chalk.dim('  未找到项目'));
    } else {
      console.log(chalk.green(`✓ 找到 ${dirs.length} 个项目:`));
      dirs.forEach(name => console.log(chalk.dim(`  - ${name}`)));
    }
  });
```

2. apps/cli/src/commands/device.ts：
```typescript
import { Command } from 'commander';
import chalk from 'chalk';

export const deviceCommand = new Command('device')
  .description('管理目标设备')
  .addHelpText('after', `
示例:
  $ openswitch device add         # 交互式添加设备
  $ openswitch device list        # 列出所有设备
`);

deviceCommand
  .command('add')
  .description('添加目标设备（交互式向导）')
  .action(async () => {
    console.log(chalk.blue('ℹ device add 命令将在 Plan 04 实现交互式向导'));
    console.log(chalk.dim('当前为命令结构骨架'));
  });

deviceCommand
  .command('list')
  .description('列出所有已配置的设备')
  .action(async () => {
    console.log(chalk.cyan('查找设备配置...'));
    console.log(chalk.dim('  设备配置功能将在后续 Plan 实现'));
  });
```

注意：create/add 命令当前只是骨架，交互式向导将在 Plan 04 实现。list 命令提供基本功能。
  </action>
  <verify>
    <automated>cd apps/cli && pnpm dev project --help && pnpm dev device --help</automated>
  </verify>
  <done>
project 和 device 子命令创建完成，命令结构清晰，帮助信息完善，为交互式向导预留入口。
  </done>
</task>

</tasks>

<verification>
1. 运行 `cd apps/cli && pnpm dev --version` 验证版本信息显示
2. 运行 `pnpm dev --help` 验证帮助信息美化输出
3. 运行 `pnpm dev workspace --help` 验证子命令帮助
4. 运行 `pnpm dev project --help` 验证子命令帮助
5. 运行 `pnpm dev device --help` 验证子命令帮助
6. 运行 `pnpm dev workspace status` 验证命令可执行
7. 验证环境检查在命令执行前自动运行（可通过临时重命名 rl 命令测试）
</verification>

<success_criteria>
- CLI 主程序实现完成，使用 Commander 框架
- 三个主要子命令（workspace、project、device）结构清晰
- 帮助系统完善，使用 chalk 美化输出，符合用户审美偏好
- 版本命令正常工作
- 环境检查在命令执行前自动运行
- 所有命令可执行，帮助信息清晰且包含使用示例
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-core/01-03-SUMMARY.md`
</output>
