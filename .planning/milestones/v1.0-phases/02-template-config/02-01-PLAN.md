---
phase: 02-template-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/schemas/template-schema.ts
  - packages/core/src/schemas/full-config-schema.ts
  - packages/core/src/schemas/index.ts
  - packages/core/src/template-manager.ts
  - packages/core/src/template-manager.test.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [CLI-TEMPLATE-01, CLI-TEMPLATE-02, CLI-TEMPLATE-04, CLI-CONFIG-01, CLI-CONFIG-02]

must_haves:
  truths:
    - "TemplateManager 可以保存四种类型的模板到 ./templates/ 目录"
    - "TemplateManager 可以列出所有模板并按类型过滤"
    - "TemplateManager 可以加载指定模板的完整内容"
    - "TemplateManager 可以删除模板（文件+索引同步）"
    - "模板内容通过 zod schema 验证"
    - "ConfigManager 可以导出完整配置为 JSON 并从 JSON 导入"
  artifacts:
    - path: "packages/core/src/schemas/template-schema.ts"
      provides: "模板元数据 schema、四种模板内容 schema、templateContentSchema"
      exports: ["templateTypeSchema", "templateMetaSchema", "templateContentSchema", "templateIndexSchema"]
    - path: "packages/core/src/schemas/full-config-schema.ts"
      provides: "完整配置 schema（workspace + projects + devices）"
      exports: ["fullConfigSchema", "FullConfig"]
    - path: "packages/core/src/template-manager.ts"
      provides: "模板 CRUD 操作类"
      exports: ["TemplateManager", "TemplateMeta", "TemplateType"]
    - path: "packages/core/src/template-manager.test.ts"
      provides: "模板管理器单元测试"
      min_lines: 80
  key_links:
    - from: "packages/core/src/template-manager.ts"
      to: "packages/core/src/schemas/template-schema.ts"
      via: "import templateContentSchema for validation"
      pattern: "import.*template-schema"
    - from: "packages/core/src/schemas/full-config-schema.ts"
      to: "packages/core/src/schemas/index.ts"
      via: "import workspace/project/device schemas"
      pattern: "import.*workspaceSchema|projectSchema|deviceSchema"
---

<objective>
实现模板系统的核心层：zod schema 定义 + TemplateManager CRUD 逻辑 + 完整配置 schema。

Purpose: 建立模板存储和验证的基础设施，供后续 InitOrchestrator 和 CLI 命令层使用。
Output: template-schema.ts, full-config-schema.ts, template-manager.ts 及其测试。
</objective>

<execution_context>
@/home/sean/.claude/get-shit-done/workflows/execute-plan.md
@/home/sean/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-template-config/02-CONTEXT.md
@.planning/phases/02-template-config/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/core/src/schemas/workspace-schema.ts:
```typescript
export const workspaceSchema = z.object({
  cwd: z.string().min(1),
  basePath: z.string().min(1),
  platform: z.enum(PLATFORM_VALUES),
  version: z.enum(['default', 'ecs_3.6.5', 'lts_3.6.5', 'lts_3.6.5_compiled', 'research', 'custom']).default('default'),
  createbase: z.boolean().default(false),
  build: z.boolean().default(false),
  debugLevel: z.enum(['debug', 'release']).default('release'),
  os: z.enum(['sylixos', 'linux']).default('sylixos')
});
export type WorkspaceConfig = z.infer<typeof workspaceSchema>;
```

From packages/core/src/schemas/project-schema.ts:
```typescript
export const projectSchema = z.object({
  name: z.string().min(3).max(50),
  template: z.enum(['app', 'lib', 'common', 'ko', ...]).optional(),
  type: z.enum(['cmake', 'automake', 'realevo', ...]).optional(),
  source: z.string().optional(),
  branch: z.string().optional(),
  debugLevel: z.enum(['debug', 'release']).optional(),
  makeTool: z.enum(['make', 'ninja']).default('make')
});
export type ProjectConfig = z.infer<typeof projectSchema>;
```

From packages/core/src/schemas/device-schema.ts:
```typescript
export const deviceSchema = z.object({
  name: z.string().min(1),
  ip: z.string().ip({ version: 'v4' }),
  platform: z.string().min(1),
  ssh: z.number().int().min(1).max(65535).default(22),
  telnet: z.number().int().min(1).max(65535).default(23),
  ftp: z.number().int().min(1).max(65535).default(21),
  gdb: z.number().int().min(1).max(65535).default(1234),
  username: z.string().min(1),
  password: z.string().optional()
});
export type DeviceConfig = z.infer<typeof deviceSchema>;
```

From packages/core/src/schemas/index.ts:
```typescript
export { workspaceSchema, type WorkspaceConfig } from './workspace-schema.js';
export { projectSchema, type ProjectConfig } from './project-schema.js';
export { deviceSchema, type DeviceConfig } from './device-schema.js';
```

From packages/core/src/config-manager.ts:
```typescript
export class ConfigManager {
  static validate<T>(schema: z.ZodSchema<T>, data: unknown): { valid: boolean; data?: T; errors?: string[] };
  static merge<T extends Record<string, any>>(...configs: Partial<T>[]): T;
  static exportToJson<T>(config: T): string;
  static importFromJson<T>(schema: z.ZodSchema<T>, json: string): { valid: boolean; data?: T; errors?: string[] };
}
```

From packages/core/src/index.ts:
```typescript
export { ConfigManager } from './config-manager.js';
export { workspaceSchema, projectSchema, deviceSchema, type WorkspaceConfig, type ProjectConfig, type DeviceConfig } from './schemas/index.js';
export { RlWrapper, executeRlCommand, type RlCommandResult, type WorkspaceInitOptions, type ProjectCreateOptions, type DeviceAddOptions } from './rl-wrapper.js';
export { ProgressReporter, type StepProgress, type ErrorEvent } from './progress-reporter.js';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema 定义 + TemplateManager 实现</name>
  <files>
    packages/core/src/schemas/template-schema.ts,
    packages/core/src/schemas/full-config-schema.ts,
    packages/core/src/schemas/index.ts,
    packages/core/src/template-manager.ts,
    packages/core/src/template-manager.test.ts,
    packages/core/src/index.ts
  </files>
  <behavior>
    - Test: slugify("ARM64 开发环境") 返回 "arm64-开发环境"（保留中文）
    - Test: slugify("  My Dev Env  ") 返回 "my-dev-env"
    - Test: slugify("test__name") 返回 "test-name"
    - Test: save workspace 模板后，templates/index.json 包含元数据，templates/{id}.json 包含内容
    - Test: save 四种类型模板（workspace/project/device/full）均通过 schema 验证
    - Test: save 同名模板时 exists() 返回 true
    - Test: list() 返回所有模板，按 updatedAt 降序排列
    - Test: list('workspace') 只返回 workspace 类型模板
    - Test: load(id) 返回 { meta, content } 结构
    - Test: load 不存在的 id 抛出错误
    - Test: delete(id) 删除文件并从 index.json 移除条目
    - Test: delete 不存在的 id 抛出错误
    - Test: fullConfigSchema 验证包含 workspace + projects + devices 的完整配置
    - Test: fullConfigSchema 验证 projects 和 devices 为 optional
    - Test: fullConfigSchema 包含 schemaVersion 字段默认值为 1
  </behavior>
  <action>
    1. 创建 `packages/core/src/schemas/template-schema.ts`:
       - `templateTypeSchema`: z.enum(['workspace', 'project', 'device', 'full'])
       - `templateMetaSchema`: z.object({ id, name, description, type, createdAt, updatedAt })
       - `templateIndexSchema`: z.object({ templates: z.array(templateMetaSchema) })
       - `templateContentSchema`: z.discriminatedUnion('type', [...]) 四种类型各自引用对应 schema

    2. 创建 `packages/core/src/schemas/full-config-schema.ts`:
       - `fullConfigSchema`: z.object({ schemaVersion: z.literal(1).default(1), workspace: workspaceSchema, projects: z.array(projectSchema).optional(), devices: z.array(deviceSchema).optional() })
       - 导出 `FullConfig` 类型

    3. 更新 `packages/core/src/schemas/index.ts`:
       - 添加 template-schema.ts 和 full-config-schema.ts 的所有导出

    4. 创建 `packages/core/src/template-manager.ts`:
       - `TemplateManager` 类，构造函数接收 baseDir（模板存储在 `{baseDir}/templates/`）
       - `static slugify(name)`: 小写、空格/下划线转连字符、保留中文和字母数字、去除首尾连字符
       - `ensureDir()`: 确保 templates/ 目录和 index.json 存在
       - `save(name, description, type, content)`: 生成 slug ID，用 templateContentSchema 验证 { type, data: content }，写入 {id}.json，更新 index.json，返回 TemplateMeta。验证失败抛出错误。
       - `list(type?)`: 读取 index.json，可选按 type 过滤，按 updatedAt 降序排序
       - `load(id)`: 读取 index.json 找到 meta，读取 {id}.json 返回 { meta, content }。不存在时抛出错误并自动清理孤立索引条目。
       - `delete(id)`: 删除 {id}.json 文件，从 index.json 移除条目。不存在时抛出错误。
       - `exists(id)`: 检查 index.json 中是否有该 ID

    5. 创建 `packages/core/src/template-manager.test.ts`:
       - 使用 vitest 的 beforeEach/afterEach 创建临时目录（os.tmpdir）
       - 覆盖上述所有 behavior 测试用例
       - 使用真实文件系统操作（不 mock fs）

    6. 更新 `packages/core/src/index.ts`:
       - 添加 TemplateManager 和相关类型的导出
       - 添加 fullConfigSchema、FullConfig 的导出
       - 添加 template schema 相关导出

    注意：所有错误消息使用中文。模板 ID 冲突处理：TemplateManager 只提供 exists() 检查，冲突交互逻辑由 CLI 层处理（per user decision）。
  </action>
  <verify>
    <automated>cd /home/sean/project/sydev && pnpm --filter @sydev/core test -- --run src/template-manager.test.ts</automated>
  </verify>
  <done>
    - template-schema.ts 导出四种模板类型的 zod schema
    - full-config-schema.ts 导出 fullConfigSchema 和 FullConfig 类型
    - TemplateManager 的 save/list/load/delete/exists/slugify 全部通过测试
    - schemas/index.ts 和 core/index.ts 导出已更新
    - 所有测试绿色
  </done>
</task>

</tasks>

<verification>
pnpm --filter @sydev/core test -- --run
</verification>

<success_criteria>
- TemplateManager CRUD 操作全部通过单元测试
- 四种模板类型（workspace/project/device/full）均可保存和加载
- fullConfigSchema 正确验证完整配置结构
- 所有新类型和类从 @sydev/core 正确导出
</success_criteria>

<output>
After completion, create `.planning/phases/02-template-config/02-01-SUMMARY.md`
</output>
