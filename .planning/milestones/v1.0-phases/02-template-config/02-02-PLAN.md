---
phase: 02-template-config
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/core/src/init-orchestrator.ts
  - packages/core/src/init-orchestrator.test.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [CLI-TEMPLATE-03, CLI-CONFIG-03]

must_haves:
  truths:
    - "InitOrchestrator 按 workspace -> projects -> devices 顺序执行全流程初始化"
    - "任何步骤失败时立即停止并返回已完成步骤列表"
    - "InitOrchestrator 通过 ProgressReporter 发出进度事件"
    - "fullConfigSchema 验证通过后才开始执行"
  artifacts:
    - path: "packages/core/src/init-orchestrator.ts"
      provides: "全流程初始化编排器"
      exports: ["InitOrchestrator", "InitResult"]
    - path: "packages/core/src/init-orchestrator.test.ts"
      provides: "编排器单元测试"
      min_lines: 60
  key_links:
    - from: "packages/core/src/init-orchestrator.ts"
      to: "packages/core/src/rl-wrapper.ts"
      via: "调用 initWorkspace/createProject/addDevice"
      pattern: "rlWrapper\\.(initWorkspace|createProject|addDevice)"
    - from: "packages/core/src/init-orchestrator.ts"
      to: "packages/core/src/progress-reporter.ts"
      via: "发出 step/success/error 事件"
      pattern: "progressReporter\\.emit|progressReporter\\.report"
    - from: "packages/core/src/init-orchestrator.ts"
      to: "packages/core/src/schemas/full-config-schema.ts"
      via: "验证输入配置"
      pattern: "import.*fullConfigSchema"
---

<objective>
实现 InitOrchestrator 编排器，按顺序执行 workspace -> projects -> devices 的全流程初始化，支持 fail-fast 和进度反馈。

Purpose: 为 `sydev init --config` 和模板应用提供核心编排逻辑。
Output: init-orchestrator.ts 及其测试。
</objective>

<execution_context>
@/home/sean/.claude/get-shit-done/workflows/execute-plan.md
@/home/sean/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-template-config/02-CONTEXT.md
@.planning/phases/02-template-config/02-RESEARCH.md
@.planning/phases/02-template-config/02-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan depends on -->

From packages/core/src/schemas/full-config-schema.ts (created in Plan 01):
```typescript
export const fullConfigSchema = z.object({
  schemaVersion: z.literal(1).default(1),
  workspace: workspaceSchema,
  projects: z.array(projectSchema).optional(),
  devices: z.array(deviceSchema).optional(),
});
export type FullConfig = z.infer<typeof fullConfigSchema>;
```

From packages/core/src/rl-wrapper.ts:
```typescript
export interface RlCommandResult {
  success: boolean;
  stdout?: string;
  stderr?: string;
  error?: string;
  fixSuggestion?: string;
}

export interface WorkspaceInitOptions {
  cwd: string; basePath: string; platform: string; version: string;
  createbase?: boolean; build?: boolean; debugLevel?: string; os?: string;
}

export interface ProjectCreateOptions {
  name: string; template?: string; type?: string; source?: string;
  branch?: string; debugLevel?: string; makeTool?: string; cwd?: string;
}

export interface DeviceAddOptions {
  name: string; ip: string; platform: string;
  ssh?: number; telnet?: number; ftp?: number; gdb?: number;
  username?: string; password?: string; cwd?: string;
}

export class RlWrapper {
  constructor(private progressReporter: ProgressReporter) {}
  async initWorkspace(config: WorkspaceInitOptions): Promise<RlCommandResult>;
  async createProject(config: ProjectCreateOptions): Promise<RlCommandResult>;
  async addDevice(config: DeviceAddOptions): Promise<RlCommandResult>;
}
```

From packages/core/src/progress-reporter.ts:
```typescript
export class ProgressReporter extends EventEmitter {
  reportStep(name: string, progress: number): void;
  reportSuccess(message: string): void;
  reportError(error: string, stack?: string, fixSuggestion?: string): void;
  reportOutput(output: string): void;
}
```

From packages/core/src/config-manager.ts:
```typescript
export class ConfigManager {
  static validate<T>(schema: z.ZodSchema<T>, data: unknown): { valid: boolean; data?: T; errors?: string[] };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: InitOrchestrator 实现</name>
  <files>
    packages/core/src/init-orchestrator.ts,
    packages/core/src/init-orchestrator.test.ts,
    packages/core/src/index.ts
  </files>
  <behavior>
    - Test: execute() 对有效的 fullConfig（仅 workspace）成功执行，返回 { success: true, completedSteps: ['workspace'] }
    - Test: execute() 对 workspace + 2 projects + 1 device 的完整配置，按顺序执行并返回所有 completedSteps
    - Test: workspace 初始化失败时立即返回 { success: false, completedSteps: [], failedStep: 'workspace', error: '...' }
    - Test: 第 2 个 project 失败时返回 { success: false, completedSteps: ['workspace', 'project:proj1'], failedStep: 'project:proj2', error: '...' }
    - Test: device 失败时 completedSteps 包含 workspace 和所有已完成的 projects
    - Test: execute() 对无效配置（schema 验证失败）直接返回错误，不调用 RlWrapper
    - Test: execute() 过程中通过 ProgressReporter 发出 step 事件（可通过 spy 验证）
    - Test: projects 和 devices 为空数组或 undefined 时正常执行（只执行 workspace）
  </behavior>
  <action>
    1. 创建 `packages/core/src/init-orchestrator.ts`:
       - 定义 `InitResult` 接口: { success: boolean, completedSteps: string[], failedStep?: string, error?: string }
       - `InitOrchestrator` 类，构造函数接收 `RlWrapper` 和 `ProgressReporter`
       - `async execute(config: unknown): Promise<InitResult>`:
         a. 先用 ConfigManager.validate(fullConfigSchema, config) 验证输入
         b. 验证失败直接返回 { success: false, completedSteps: [], failedStep: 'validation', error: 验证错误信息 }
         c. 调用 rlWrapper.initWorkspace(config.workspace)，失败则 fail-fast
         d. 顺序遍历 config.projects，每个调用 rlWrapper.createProject({ ...project, cwd: config.workspace.cwd })，失败则 fail-fast
         e. 顺序遍历 config.devices，每个调用 rlWrapper.addDevice({ ...device, cwd: config.workspace.cwd })，失败则 fail-fast
         f. 每个步骤前后通过 progressReporter 报告进度
         g. 全部成功返回 { success: true, completedSteps }

    2. 创建 `packages/core/src/init-orchestrator.test.ts`:
       - Mock RlWrapper（不实际调用 rl 命令）：创建 mock 对象，initWorkspace/createProject/addDevice 返回可控的 Promise
       - 使用真实的 ProgressReporter 实例，通过 vi.spyOn 或 event listener 验证事件发出
       - 覆盖上述所有 behavior 测试用例

    3. 更新 `packages/core/src/index.ts`:
       - 添加 InitOrchestrator 和 InitResult 的导出

    注意：InitOrchestrator 接收 unknown 类型的 config 并内部验证，这样 CLI 层不需要预先解析类型。所有错误消息使用中文。
  </action>
  <verify>
    <automated>cd /home/sean/project/sydev && pnpm --filter @sydev/core test -- --run src/init-orchestrator.test.ts</automated>
  </verify>
  <done>
    - InitOrchestrator 按 workspace -> projects -> devices 顺序执行
    - 任何步骤失败立即停止，返回已完成步骤和失败信息
    - 无效配置在执行前被拦截
    - ProgressReporter 事件正确发出
    - 所有测试绿色
  </done>
</task>

</tasks>

<verification>
pnpm --filter @sydev/core test -- --run
</verification>

<success_criteria>
- InitOrchestrator 的 execute() 方法通过所有单元测试
- fail-fast 行为正确：失败时返回已完成步骤列表
- 配置验证在执行前完成
- 从 @sydev/core 正确导出 InitOrchestrator 和 InitResult
</success_criteria>

<output>
After completion, create `.planning/phases/02-template-config/02-02-SUMMARY.md`
</output>
