---
phase: 01-cli-core
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/cli/src/wizards/device-wizard.ts
  - packages/core/src/constants.ts
autonomous: true
gap_closure: true
requirements: [CLI-WIZARD-03]

must_haves:
  truths:
    - "用户运行 device add 时首先选择 workspace 路径（默认当前目录）"
    - "用户通过列表选择平台，而非自由文本输入"
    - "平台选择列表与 workspace wizard 使用相同的 PLATFORMS 常量"
  artifacts:
    - path: "apps/cli/src/wizards/device-wizard.ts"
      provides: "更新后的设备配置向导，支持 workspace 路径选择和平台列表选择"
      min_lines: 150
    - path: "packages/core/src/constants.ts"
      provides: "共享的 PLATFORMS 常量定义"
      exports: ["PLATFORMS"]
  key_links:
    - from: "apps/cli/src/wizards/device-wizard.ts"
      to: "packages/core/src/constants.ts"
      via: "导入 PLATFORMS 常量"
      pattern: "import.*PLATFORMS.*from.*@sydev/core"
    - from: "apps/cli/src/wizards/workspace-wizard.ts"
      to: "packages/core/src/constants.ts"
      via: "导入 PLATFORMS 常量"
      pattern: "import.*PLATFORMS.*from.*@sydev/core"
---

<objective>
修复 Device 配置向导，添加 workspace 路径选择步骤，并将平台输入改为列表选择。

Purpose: 修复 UAT Test 7 发现的阻塞问题，使设备配置流程与 workspace wizard 保持一致
Output: 更新后的 device wizard 和共享的 PLATFORMS 常量
</objective>

<execution_context>
@/home/sean/.claude/get-shit-done/workflows/execute-plan.md
@/home/sean/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cli-core/01-04-SUMMARY.md
@.planning/phases/01-cli-core/01-UAT.md

<interfaces>
<!-- workspace-wizard.ts 中的 PLATFORMS 常量定义 -->
From apps/cli/src/wizards/workspace-wizard.ts (lines 14-78):
```typescript
const PLATFORMS = [
  { name: 'ARM --- ARM_926H', value: 'ARM_926H' },
  { name: 'ARM --- ARM_926S', value: 'ARM_926S' },
  // ... 共 56 个平台选项
  { name: 'SW6B -- SW6B_SOFT', value: 'SW6B_SOFT' }
];
```

<!-- workspace-wizard.ts 中的路径选择模式 -->
From apps/cli/src/wizards/workspace-wizard.ts (lines 86-96):
```typescript
{
  type: 'input',
  name: 'cwd',
  message: 'Workspace 创建路径:',
  default: process.cwd(),
  validate: (input: string) => {
    if (!input || input.trim().length === 0) {
      return '工作路径不能为空';
    }
    return true;
  }
}
```

<!-- workspace-wizard.ts 中的平台选择模式 -->
From apps/cli/src/wizards/workspace-wizard.ts (lines 150-155):
```typescript
{
  type: 'list',
  name: 'platform',
  message: '目标平台:',
  choices: PLATFORMS,
  default: 'ARM64_GENERIC'
}
```

<!-- device-wizard.ts 当前的平台输入（需要改为 list） -->
From apps/cli/src/wizards/device-wizard.ts (lines 42-52):
```typescript
{
  type: 'input',  // 需要改为 'list'
  name: 'platform',
  message: '设备平台:',
  validate: (input: string) => {
    if (!input || input.trim().length === 0) {
      return '平台不能为空';
    }
    return true;
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 提取 PLATFORMS 常量到共享位置</name>
  <files>packages/core/src/constants.ts, packages/core/src/index.ts</files>
  <action>
1. 创建 packages/core/src/constants.ts 文件
2. 从 apps/cli/src/wizards/workspace-wizard.ts (lines 14-78) 复制 PLATFORMS 常量定义到新文件
3. 导出 PLATFORMS 常量：
   ```typescript
   export const PLATFORMS = [
     { name: 'ARM --- ARM_926H', value: 'ARM_926H' },
     // ... 所有 56 个平台选项
   ];
   ```
4. 在 packages/core/src/index.ts 中添加导出：
   ```typescript
   export { PLATFORMS } from './constants.js';
   ```

这样 workspace-wizard 和 device-wizard 都可以从 @sydev/core 导入 PLATFORMS。
  </action>
  <verify>
    <automated>cd /home/sean/project/openswitch && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>packages/core/src/constants.ts 存在并导出 PLATFORMS，packages/core/src/index.ts 重新导出 PLATFORMS，TypeScript 编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 更新 workspace-wizard 使用共享的 PLATFORMS</name>
  <files>apps/cli/src/wizards/workspace-wizard.ts</files>
  <action>
1. 移除 workspace-wizard.ts 中的本地 PLATFORMS 常量定义（lines 14-78）
2. 在文件顶部添加导入：
   ```typescript
   import { PLATFORMS } from '@sydev/core';
   ```
3. 保持其他代码不变，PLATFORMS 的使用方式不变（line 153）

这样确保 workspace-wizard 和 device-wizard 使用相同的平台列表。
  </action>
  <verify>
    <automated>cd /home/sean/project/openswitch && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>workspace-wizard.ts 从 @sydev/core 导入 PLATFORMS，不再包含本地定义，TypeScript 编译通过</done>
</task>

<task type="auto">
  <name>Task 3: 更新 device-wizard 添加 workspace 路径选择和平台列表选择</name>
  <files>apps/cli/src/wizards/device-wizard.ts</files>
  <action>
1. 在文件顶部添加导入：
   ```typescript
   import { PLATFORMS } from '@sydev/core';
   ```

2. 在 inquirer.prompt() 的问题数组最前面添加 workspace 路径选择（参考 workspace-wizard.ts line 86-96）：
   ```typescript
   {
     type: 'input',
     name: 'cwd',
     message: 'Workspace 路径:',
     default: process.cwd(),
     validate: (input: string) => {
       if (!input || input.trim().length === 0) {
         return '工作路径不能为空';
       }
       return true;
     }
   }
   ```

3. 将平台字段从 type: 'input' 改为 type: 'list'（当前 lines 42-52）：
   ```typescript
   {
     type: 'list',
     name: 'platform',
     message: '设备平台:',
     choices: PLATFORMS,
     default: 'ARM64_GENERIC'
   }
   ```
   移除原有的 validate 函数（列表选择不需要验证）。

4. 在配置摘要中添加 workspace 路径显示（在 line 145 之前）：
   ```typescript
   console.log(chalk.dim(`  Workspace: ${answers.cwd}`));
   ```

5. 在调用 rlWrapper.addDevice() 时传递 cwd 参数（如果 RlWrapper 支持）。如果 addDevice() 不支持 cwd 参数，则仅在摘要中显示，不传递给 RlWrapper。
  </action>
  <verify>
    <automated>cd /home/sean/project/openswitch && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>device-wizard.ts 首先询问 workspace 路径，平台使用列表选择（与 workspace wizard 相同的 PLATFORMS），TypeScript 编译通过</done>
</task>

<task type="auto">
  <name>Task 4: 验证更新后的 device wizard</name>
  <files>packages/core/src/config-manager.test.ts</files>
  <action>
运行现有测试套件，确保：
1. PLATFORMS 常量可以从 @sydev/core 正确导入
2. workspace-wizard 和 device-wizard 都能正常工作
3. 所有相关测试通过

如果测试失败，更新测试用例以匹配新的导入路径。
  </action>
  <verify>
    <automated>cd /home/sean/project/openswitch && pnpm test</automated>
  </verify>
  <done>所有测试通过，device wizard 更新完成，UAT Test 7 的阻塞问题解除</done>
</task>

</tasks>

<verification>
1. 运行 `pnpm dev:cli device add` 验证交互流程：
   - 首先询问 workspace 路径（默认当前目录）
   - 平台字段使用列表选择，显示所有 56 个平台选项
   - 配置摘要包含 workspace 路径
2. 运行 `pnpm dev:cli workspace init` 验证 workspace wizard 仍然正常工作
3. TypeScript 编译通过
4. 所有测试通过
</verification>

<success_criteria>
- PLATFORMS 常量提取到 packages/core/src/constants.ts 并被两个 wizard 共享
- device wizard 首先询问 workspace 路径（默认当前目录）
- device wizard 使用列表选择平台，而非自由文本输入
- workspace wizard 和 device wizard 使用相同的平台列表
- UAT Test 7 的阻塞问题解除
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-core/01-08-SUMMARY.md`
</output>
