---
phase: 01-cli-core
plan: 09
type: execute
wave: 2
depends_on: [01-07, 01-08]
files_modified:
  - packages/core/src/rl-wrapper.ts
  - apps/cli/src/wizards/project-wizard.ts
  - apps/cli/src/wizards/device-wizard.ts
autonomous: true
gap_closure: true
requirements: [CLI-WIZARD-02, CLI-WIZARD-03]

must_haves:
  truths:
    - "rl-project 命令在用户指定的 workspace 目录下执行"
    - "rl-device 命令在用户指定的 workspace 目录下执行"
    - "用户不再看到 'Go to the workspace directory and run this command' 错误"
  artifacts:
    - path: "packages/core/src/rl-wrapper.ts"
      provides: "createProject() 和 addDevice() 方法接受 cwd 参数"
      exports: ["createProject", "addDevice"]
    - path: "apps/cli/src/wizards/project-wizard.ts"
      provides: "调用 createProject() 时传递 phase1.cwd"
      min_lines: 250
    - path: "apps/cli/src/wizards/device-wizard.ts"
      provides: "调用 addDevice() 时传递 answers.cwd"
      min_lines: 190
  key_links:
    - from: "apps/cli/src/wizards/project-wizard.ts"
      to: "RlWrapper.createProject()"
      via: "传递 phase1.cwd 参数"
      pattern: "createProject\\(\\{[^}]*cwd:"
    - from: "apps/cli/src/wizards/device-wizard.ts"
      to: "RlWrapper.addDevice()"
      via: "传递 answers.cwd 参数"
      pattern: "addDevice\\(\\{[^}]*cwd:"
    - from: "RlWrapper.createProject()"
      to: "executeRlCommand()"
      via: "传递 cwd 到 rl-project 命令"
      pattern: "executeRlCommand\\('rl-project',.*,.*,.*cwd"
    - from: "RlWrapper.addDevice()"
      to: "executeRlCommand()"
      via: "传递 cwd 到 rl-device 命令"
      pattern: "executeRlCommand\\('rl-device',.*,.*,.*cwd"
---

<objective>
修复 project 和 device 向导收集的 workspace 路径未被实际使用的问题，确保 rl 命令在正确的目录下执行。

Purpose: 解除 UAT 测试中 "Go to the workspace directory and run this command" 错误，使向导收集的 workspace 路径真正生效。

Output: RlWrapper 方法接受 cwd 参数，向导正确传递路径，rl 命令在指定目录执行。
</objective>

<execution_context>
@/home/sean/.claude/get-shit-done/workflows/execute-plan.md
@/home/sean/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cli-core/01-UAT.md
@.planning/phases/01-cli-core/01-07-SUMMARY.md
@.planning/phases/01-cli-core/01-08-SUMMARY.md
</context>

<interfaces>
<!-- Key interfaces from existing code -->

From packages/core/src/rl-wrapper.ts:
```typescript
export interface ProjectCreateOptions {
  name: string;
  template?: string;
  type?: string;
  source?: string;
  branch?: string;
  debugLevel?: string;
  makeTool?: string;
}

export interface DeviceAddOptions {
  name: string;
  ip: string;
  platform: string;
  ssh?: number;
  telnet?: number;
  ftp?: number;
  gdb?: number;
  username?: string;
  password?: string;
}

export class RlWrapper {
  async createProject(config: ProjectCreateOptions): Promise<RlCommandResult>;
  async addDevice(config: DeviceAddOptions): Promise<RlCommandResult>;
}

export async function executeRlCommand(
  command: string,
  args: string[],
  progressReporter?: ProgressReporter,
  cwd?: string  // Already supports cwd parameter
): Promise<RlCommandResult>;
```

From apps/cli/src/wizards/project-wizard.ts:
```typescript
// Line 15-27: phase1 collects cwd
const phase1 = await inquirer.prompt([
  {
    type: 'input',
    name: 'cwd',
    message: 'Workspace 路径:',
    default: process.cwd(),
    // ...
  }
]);

// Line 244-252: createProject() called without cwd
const result = await rlWrapper.createProject({
  name: config.name,
  template: config.template,
  // ... but no cwd passed
});
```

From apps/cli/src/wizards/device-wizard.ts:
```typescript
// Line 15-27: answers collects cwd
const answers = await inquirer.prompt([
  {
    type: 'input',
    name: 'cwd',
    message: 'Workspace 路径:',
    default: process.cwd(),
    // ...
  }
]);

// Line 183-193: addDevice() called without cwd
const result = await rlWrapper.addDevice({
  name: config.name,
  ip: config.ip,
  // ... but no cwd passed
});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 为 RlWrapper 方法添加 cwd 参数</name>
  <files>packages/core/src/rl-wrapper.ts</files>
  <action>
修改 `ProjectCreateOptions` 和 `DeviceAddOptions` 接口，添加可选的 `cwd?: string` 字段。

修改 `RlWrapper.createProject()` 方法：
- 从 `config.cwd` 读取工作目录
- 调用 `executeRlCommand('rl-project', args, this.progressReporter, config.cwd)` 时传递 cwd 参数（第 4 个参数）

修改 `RlWrapper.addDevice()` 方法：
- 从 `config.cwd` 读取工作目录
- 调用 `executeRlCommand('rl-device', args, this.progressReporter, config.cwd)` 时传递 cwd 参数（第 4 个参数）

注意：`executeRlCommand()` 已经支持 cwd 参数（line 16），无需修改该函数。
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm test</automated>
  </verify>
  <done>ProjectCreateOptions 和 DeviceAddOptions 包含 cwd 字段，createProject() 和 addDevice() 将 cwd 传递给 executeRlCommand()，所有测试通过</done>
</task>

<task type="auto">
  <name>Task 2: 更新 project-wizard 传递 cwd 参数</name>
  <files>apps/cli/src/wizards/project-wizard.ts</files>
  <action>
修改 `runProjectWizard()` 函数中调用 `rlWrapper.createProject()` 的代码（line 244-252）：

在传递给 `createProject()` 的配置对象中添加 `cwd: phase1.cwd` 字段。

修改前：
```typescript
const result = await rlWrapper.createProject({
  name: config.name,
  template: config.template,
  type: config.type,
  source: config.source,
  branch: config.branch,
  debugLevel: config.debugLevel,
  makeTool: config.makeTool
});
```

修改后：
```typescript
const result = await rlWrapper.createProject({
  name: config.name,
  template: config.template,
  type: config.type,
  source: config.source,
  branch: config.branch,
  debugLevel: config.debugLevel,
  makeTool: config.makeTool,
  cwd: phase1.cwd  // 传递用户选择的 workspace 路径
});
```
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && grep -q "cwd: phase1.cwd" apps/cli/src/wizards/project-wizard.ts</automated>
  </verify>
  <done>project-wizard 调用 createProject() 时传递 phase1.cwd，rl-project 命令将在指定目录执行</done>
</task>

<task type="auto">
  <name>Task 3: 更新 device-wizard 传递 cwd 参数</name>
  <files>apps/cli/src/wizards/device-wizard.ts</files>
  <action>
修改 `runDeviceWizard()` 函数中调用 `rlWrapper.addDevice()` 的代码（line 183-193）：

在传递给 `addDevice()` 的配置对象中添加 `cwd: answers.cwd` 字段。

修改前：
```typescript
const result = await rlWrapper.addDevice({
  name: config.name,
  ip: config.ip,
  platform: config.platform,
  ssh: config.ssh,
  telnet: config.telnet,
  ftp: config.ftp,
  gdb: config.gdb,
  username: config.username,
  password: config.password
});
```

修改后：
```typescript
const result = await rlWrapper.addDevice({
  name: config.name,
  ip: config.ip,
  platform: config.platform,
  ssh: config.ssh,
  telnet: config.telnet,
  ftp: config.ftp,
  gdb: config.gdb,
  username: config.username,
  password: config.password,
  cwd: answers.cwd  // 传递用户选择的 workspace 路径
});
```
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && grep -q "cwd: answers.cwd" apps/cli/src/wizards/device-wizard.ts</automated>
  </verify>
  <done>device-wizard 调用 addDevice() 时传递 answers.cwd，rl-device 命令将在指定目录执行</done>
</task>

</tasks>

<verification>
## 整体验证

1. **类型系统验证：** TypeScript 编译通过，无类型错误
2. **测试套件验证：** 所有单元测试通过
3. **代码路径验证：** grep 确认 cwd 参数在所有关键位置正确传递
4. **UAT 影响：** 修复后用户运行 `project create` 和 `device add` 时，rl 命令将在用户指定的 workspace 目录下执行，不再出现 "Go to the workspace directory and run this command" 错误

## 关键检查点

```bash
# 验证接口定义包含 cwd
grep -n "cwd?: string" packages/core/src/rl-wrapper.ts

# 验证 createProject 传递 cwd
grep -n "executeRlCommand('rl-project'.*config.cwd" packages/core/src/rl-wrapper.ts

# 验证 addDevice 传递 cwd
grep -n "executeRlCommand('rl-device'.*config.cwd" packages/core/src/rl-wrapper.ts

# 验证 project-wizard 传递 cwd
grep -n "cwd: phase1.cwd" apps/cli/src/wizards/project-wizard.ts

# 验证 device-wizard 传递 cwd
grep -n "cwd: answers.cwd" apps/cli/src/wizards/device-wizard.ts
```
</verification>

<success_criteria>
1. `ProjectCreateOptions` 和 `DeviceAddOptions` 接口包含 `cwd?: string` 字段
2. `RlWrapper.createProject()` 将 `config.cwd` 传递给 `executeRlCommand()`
3. `RlWrapper.addDevice()` 将 `config.cwd` 传递给 `executeRlCommand()`
4. `project-wizard.ts` 调用 `createProject()` 时传递 `cwd: phase1.cwd`
5. `device-wizard.ts` 调用 `addDevice()` 时传递 `cwd: answers.cwd`
6. TypeScript 编译通过，无类型错误
7. 所有单元测试通过
8. UAT Test 6 的 "Go to the workspace directory" 错误不再出现
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-core/01-09-SUMMARY.md`
</output>
