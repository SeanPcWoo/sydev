---
phase: 01-cli-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/config-manager.ts
  - packages/core/src/config-manager.test.ts
  - packages/core/src/rl-wrapper.ts
  - packages/core/src/rl-wrapper.test.ts
  - packages/core/src/progress-reporter.ts
  - packages/core/src/progress-reporter.test.ts
  - packages/core/src/schemas/workspace-schema.ts
  - packages/core/src/schemas/project-schema.ts
  - packages/core/src/schemas/device-schema.ts
  - packages/core/package.json
autonomous: true
requirements:
  - CLI-FEEDBACK-01
  - CLI-FEEDBACK-02
  - CLI-FEEDBACK-03

must_haves:
  truths:
    - "用户在初始化过程中看到实时进度反馈（当前步骤、进度百分比）"
    - "用户在每个步骤完成时看到成功提示"
    - "用户在操作失败时看到清晰的错误信息和堆栈跟踪"
  artifacts:
    - path: "packages/core/src/config-manager.ts"
      provides: "配置管理器，支持验证、合并、导入导出"
      exports: ["ConfigManager"]
      min_lines: 100
    - path: "packages/core/src/rl-wrapper.ts"
      provides: "rl 命令包装器，错误解析和友好提示"
      exports: ["RlWrapper", "executeRlCommand"]
      min_lines: 80
    - path: "packages/core/src/progress-reporter.ts"
      provides: "进度报告器，基于 EventEmitter"
      exports: ["ProgressReporter"]
      min_lines: 60
    - path: "packages/core/src/schemas/workspace-schema.ts"
      provides: "Workspace 配置 JSON Schema"
      exports: ["workspaceSchema"]
      min_lines: 30
  key_links:
    - from: "packages/core/src/config-manager.ts"
      to: "packages/core/src/schemas/workspace-schema.ts"
      via: "import schema and validate with zod"
      pattern: "import.*workspaceSchema.*parse"
    - from: "packages/core/src/rl-wrapper.ts"
      to: "packages/core/src/progress-reporter.ts"
      via: "emit progress events"
      pattern: "progressReporter\\.emit"
    - from: "packages/core/src/rl-wrapper.ts"
      to: "child_process"
      via: "spawn rl commands"
      pattern: "spawn.*rl"
---

<objective>
实现核心业务逻辑模块：配置管理器（验证和合并配置）、rl 命令包装器（封装 RealEvo-Stream 命令调用）、进度报告器（解耦的事件驱动进度反馈）。

Purpose: 建立可靠的配置处理和命令执行基础设施，为后续的交互式向导和模板系统提供稳定的底层 API。进度报告器确保用户在长时间操作中获得实时反馈。

Output: 三个核心模块及其测试，所有模块通过 @openswitch/core 导出供 CLI 和未来的 Web UI 使用。
</objective>

<execution_context>
@/home/sean/.claude/get-shit-done/workflows/execute-plan.md
@/home/sean/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现配置管理器和 JSON Schema</name>
  <files>
    packages/core/src/schemas/workspace-schema.ts
    packages/core/src/schemas/project-schema.ts
    packages/core/src/schemas/device-schema.ts
    packages/core/src/config-manager.ts
    packages/core/src/config-manager.test.ts
    packages/core/package.json
  </files>
  <behavior>
    - Test 1: workspaceSchema 验证有效的 workspace 配置（baseVersion, platform, buildOptions）
    - Test 2: workspaceSchema 拒绝无效配置（缺少必填字段、类型错误）
    - Test 3: ConfigManager.validate() 使用 schema 验证配置并返回详细错误
    - Test 4: ConfigManager.merge() 合并多个配置对象，后者覆盖前者
    - Test 5: ConfigManager.exportToJson() 导出配置为 JSON 字符串
    - Test 6: ConfigManager.importFromJson() 从 JSON 字符串导入并验证配置
  </behavior>
  <action>
在 packages/core/src/ 创建配置管理模块：

1. 添加 zod 依赖到 packages/core/package.json：
   - dependencies: { "zod": "^3.23.0" }

2. schemas/workspace-schema.ts 定义 Workspace 配置 schema：
```typescript
import { z } from 'zod';

export const workspaceSchema = z.object({
  baseVersion: z.string().min(1, "Base 版本不能为空"),
  platform: z.enum(['arm', 'x86', 'mips', 'riscv'], {
    errorMap: () => ({ message: "平台必须是 arm, x86, mips 或 riscv" })
  }),
  buildOptions: z.object({
    debug: z.boolean().default(false),
    optimize: z.enum(['none', 'size', 'speed']).default('none')
  }).optional(),
  path: z.string().optional()
});

export type WorkspaceConfig = z.infer<typeof workspaceSchema>;
```

3. schemas/project-schema.ts 定义 Project 配置 schema：
```typescript
export const projectSchema = z.object({
  name: z.string().min(3, "项目名称至少 3 个字符").max(50, "项目名称最多 50 个字符"),
  type: z.enum(['app', 'lib', 'driver']),
  template: z.string().optional(),
  path: z.string().optional()
});

export type ProjectConfig = z.infer<typeof projectSchema>;
```

4. schemas/device-schema.ts 定义 Device 配置 schema：
```typescript
export const deviceSchema = z.object({
  name: z.string().min(1),
  ip: z.string().ip({ version: 'v4', message: "必须是有效的 IPv4 地址" }),
  port: z.number().int().min(1).max(65535).default(22),
  username: z.string().min(1),
  password: z.string().optional()
});

export type DeviceConfig = z.infer<typeof deviceSchema>;
```

5. config-manager.ts 实现配置管理器：
```typescript
import { z } from 'zod';
import { workspaceSchema, projectSchema, deviceSchema } from './schemas/index.js';

export class ConfigManager {
  static validate<T>(schema: z.ZodSchema<T>, data: unknown): { valid: boolean; data?: T; errors?: string[] } {
    const result = schema.safeParse(data);
    if (result.success) {
      return { valid: true, data: result.data };
    }
    return {
      valid: false,
      errors: result.error.errors.map(e => `${e.path.join('.')}: ${e.message}`)
    };
  }

  static merge<T extends Record<string, any>>(...configs: Partial<T>[]): T {
    return Object.assign({}, ...configs) as T;
  }

  static exportToJson<T>(config: T): string {
    return JSON.stringify(config, null, 2);
  }

  static importFromJson<T>(schema: z.ZodSchema<T>, json: string): { valid: boolean; data?: T; errors?: string[] } {
    try {
      const parsed = JSON.parse(json);
      return ConfigManager.validate(schema, parsed);
    } catch (error) {
      return { valid: false, errors: [`JSON 解析失败: ${error.message}`] };
    }
  }
}
```

6. config-manager.test.ts 使用 Vitest 测试所有场景。

使用 TDD：先写测试（RED），再实现（GREEN）。
  </action>
  <verify>
    <automated>cd packages/core && pnpm test config-manager.test.ts</automated>
  </verify>
  <done>
配置管理器实现完成，所有测试通过，能够验证、合并、导入导出配置，错误信息清晰且中文化。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 rl 命令包装器</name>
  <files>
    packages/core/src/rl-wrapper.ts
    packages/core/src/rl-wrapper.test.ts
  </files>
  <behavior>
    - Test 1: executeRlCommand() 成功执行 rl 命令并返回输出
    - Test 2: executeRlCommand() 在命令失败时解析错误并返回友好提示
    - Test 3: RlWrapper.initWorkspace() 调用 `rl workspace init` 并发出进度事件
    - Test 4: RlWrapper.createProject() 调用 `rl project create` 并处理错误
    - Test 5: 错误解析器识别常见错误模式（权限错误、路径不存在、版本不兼容）并提供修复建议
  </behavior>
  <action>
在 packages/core/src/ 创建 rl 命令包装器：

1. rl-wrapper.ts 实现命令包装：
```typescript
import { spawn } from 'child_process';
import { ProgressReporter } from './progress-reporter.js';

export interface RlCommandResult {
  success: boolean;
  stdout?: string;
  stderr?: string;
  error?: string;
  fixSuggestion?: string;
}

export async function executeRlCommand(
  command: string,
  args: string[],
  progressReporter?: ProgressReporter
): Promise<RlCommandResult> {
  return new Promise((resolve) => {
    const proc = spawn('rl', [command, ...args]);
    let stdout = '';
    let stderr = '';

    proc.stdout?.on('data', (data) => {
      stdout += data.toString();
      progressReporter?.emit('output', data.toString());
    });

    proc.stderr?.on('data', (data) => {
      stderr += data.toString();
    });

    proc.on('close', (code) => {
      if (code === 0) {
        resolve({ success: true, stdout });
      } else {
        const { error, fixSuggestion } = parseRlError(stderr || stdout);
        resolve({ success: false, stderr, error, fixSuggestion });
      }
    });

    proc.on('error', (err) => {
      resolve({
        success: false,
        error: `执行 rl 命令失败: ${err.message}`,
        fixSuggestion: '请确认 RealEvo-Stream 已正确安装且 rl 命令在 PATH 中'
      });
    });
  });
}

function parseRlError(output: string): { error: string; fixSuggestion: string } {
  // 识别常见错误模式
  if (output.includes('permission denied') || output.includes('权限不足')) {
    return {
      error: '权限不足',
      fixSuggestion: '请使用 sudo 运行或检查文件权限'
    };
  }
  if (output.includes('not found') || output.includes('不存在')) {
    return {
      error: '路径或文件不存在',
      fixSuggestion: '请检查路径是否正确，或先创建必要的目录'
    };
  }
  if (output.includes('version') || output.includes('版本')) {
    return {
      error: '版本不兼容',
      fixSuggestion: '请升级 RealEvo-Stream 到最新版本'
    };
  }
  return {
    error: output.trim() || '未知错误',
    fixSuggestion: '请查看错误信息或联系技术支持'
  };
}

export class RlWrapper {
  constructor(private progressReporter: ProgressReporter) {}

  async initWorkspace(config: { baseVersion: string; platform: string; path: string }): Promise<RlCommandResult> {
    this.progressReporter.emit('step', { name: '初始化 Workspace', progress: 0 });
    const result = await executeRlCommand(
      'workspace',
      ['init', '--base', config.baseVersion, '--platform', config.platform, '--path', config.path],
      this.progressReporter
    );
    if (result.success) {
      this.progressReporter.emit('step', { name: '初始化 Workspace', progress: 100 });
    }
    return result;
  }

  async createProject(config: { name: string; type: string; path: string }): Promise<RlCommandResult> {
    this.progressReporter.emit('step', { name: `创建项目 ${config.name}`, progress: 0 });
    const result = await executeRlCommand(
      'project',
      ['create', '--name', config.name, '--type', config.type, '--path', config.path],
      this.progressReporter
    );
    if (result.success) {
      this.progressReporter.emit('step', { name: `创建项目 ${config.name}`, progress: 100 });
    }
    return result;
  }
}
```

2. rl-wrapper.test.ts 使用 Vitest：
   - Mock child_process.spawn 测试不同场景
   - 验证进度事件正确发出
   - 测试错误解析和修复建议

使用 TDD：先写测试（RED），再实现（GREEN）。
  </action>
  <verify>
    <automated>cd packages/core && pnpm test rl-wrapper.test.ts</automated>
  </verify>
  <done>
rl 命令包装器实现完成，所有测试通过，能够执行 rl 命令、解析错误、发出进度事件，错误提示清晰且可操作。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现进度报告器</name>
  <files>
    packages/core/src/progress-reporter.ts
    packages/core/src/progress-reporter.test.ts
  </files>
  <behavior>
    - Test 1: ProgressReporter 继承 EventEmitter，支持 on/emit
    - Test 2: emit('step') 发出步骤进度事件，监听器接收 { name, progress }
    - Test 3: emit('success') 发出成功事件，监听器接收消息
    - Test 4: emit('error') 发出错误事件，监听器接收 { error, stack }
    - Test 5: 多个监听器可以同时订阅同一事件
  </behavior>
  <action>
在 packages/core/src/ 创建进度报告器：

1. progress-reporter.ts 实现事件驱动的进度报告：
```typescript
import { EventEmitter } from 'events';

export interface StepProgress {
  name: string;
  progress: number; // 0-100
}

export interface ErrorEvent {
  error: string;
  stack?: string;
  fixSuggestion?: string;
}

export class ProgressReporter extends EventEmitter {
  reportStep(name: string, progress: number): void {
    this.emit('step', { name, progress });
  }

  reportSuccess(message: string): void {
    this.emit('success', message);
  }

  reportError(error: string, stack?: string, fixSuggestion?: string): void {
    this.emit('error', { error, stack, fixSuggestion });
  }

  reportOutput(output: string): void {
    this.emit('output', output);
  }
}
```

2. progress-reporter.test.ts 使用 Vitest：
   - 测试事件发出和监听
   - 验证多个监听器可以同时订阅
   - 测试事件数据结构正确

使用 TDD：先写测试（RED），再实现（GREEN）。
  </action>
  <verify>
    <automated>cd packages/core && pnpm test progress-reporter.test.ts</automated>
  </verify>
  <done>
进度报告器实现完成，所有测试通过，支持步骤进度、成功、错误、输出四种事件类型，解耦的事件驱动架构。
  </done>
</task>

<task type="auto">
  <name>Task 4: 更新核心包导出索引</name>
  <files>
    packages/core/src/index.ts
    packages/core/src/schemas/index.ts
  </files>
  <action>
更新 packages/core/src/index.ts 导出新模块：

```typescript
// 环境检查
export {
  checkRlCommand,
  checkToolchain,
  checkEnvironment
} from './env-checker.js';

// 配置管理
export { ConfigManager } from './config-manager.js';
export {
  workspaceSchema,
  projectSchema,
  deviceSchema,
  type WorkspaceConfig,
  type ProjectConfig,
  type DeviceConfig
} from './schemas/index.js';

// rl 命令包装
export {
  RlWrapper,
  executeRlCommand,
  type RlCommandResult
} from './rl-wrapper.js';

// 进度报告
export {
  ProgressReporter,
  type StepProgress,
  type ErrorEvent
} from './progress-reporter.js';

// 类型定义
export type {
  RlVersion,
  EnvCheckResult,
  ToolchainCheckResult,
  EnvironmentStatus
} from './types.js';
```

创建 packages/core/src/schemas/index.ts：
```typescript
export { workspaceSchema, type WorkspaceConfig } from './workspace-schema.js';
export { projectSchema, type ProjectConfig } from './project-schema.js';
export { deviceSchema, type DeviceConfig } from './device-schema.js';
```
  </action>
  <verify>
    <automated>cd packages/core && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>
核心包导出索引更新完成，所有新模块正确导出，TypeScript 编译检查通过。
  </done>
</task>

</tasks>

<verification>
1. 运行 `cd packages/core && pnpm test` 验证所有测试通过
2. 运行 `pnpm exec tsc --noEmit` 验证 TypeScript 类型检查通过
3. 检查 zod 依赖正确安装
4. 验证配置 schema 能够正确验证和拒绝无效配置
5. 验证 rl 命令包装器能够解析错误并提供修复建议
6. 验证进度报告器事件正确发出和接收
</verification>

<success_criteria>
- 配置管理器实现完成，支持验证、合并、导入导出，所有测试通过
- rl 命令包装器实现完成，支持命令执行、错误解析、进度报告，所有测试通过
- 进度报告器实现完成，基于 EventEmitter，支持多种事件类型，所有测试通过
- 所有模块通过 @openswitch/core 正确导出
- TypeScript 编译无错误
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-core/01-02-SUMMARY.md`
</output>
